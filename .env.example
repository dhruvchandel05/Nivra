# Used by docker-compose.yml (copy to .env next to it)
CLIENT_PORT=8080

# "development" works over plain http://localhost.
# Use "production" only behind HTTPS (session cookie is Secure in production).
NODE_ENV=development
